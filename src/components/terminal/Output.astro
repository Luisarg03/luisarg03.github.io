<div class="terminal-output" id="terminal-output">
  <slot />
</div>

<style>
  .terminal-output {
    flex: 1;
    overflow-y: auto;
    padding: 0.5rem 1rem;
  }

  .terminal-output :global(.output-line) {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    line-height: 1.6;
    white-space: pre-wrap;
    word-break: break-word;
    margin: 0;
  }

  .terminal-output :global(.output-command) {
    color: var(--color-text-muted);
  }

  .terminal-output :global(.output-text) {
    color: var(--color-text);
  }

  .terminal-output :global(.output-error) {
    color: var(--color-danger);
  }

  .terminal-output :global(.output-info) {
    color: var(--color-accent);
  }

  .terminal-output :global(.output-success) {
    color: var(--color-success);
  }

  .terminal-output :global(.output-warning) {
    color: var(--color-warning);
  }
</style>
