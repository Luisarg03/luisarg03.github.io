---
title: "SageMaker CI/CD – PoC"
description: "PoC que valida un flujo CI/CD completo para pipelines de modelos ML sobre SageMaker sin cuenta AWS real: GitHub Actions + Ministack (S3/SageMaker simulados en el runner), convención una-branch-por-modelo."
repo: "https://github.com/Luisarg03/sagemaker-cicd-poc"
problem: "Flujo CI/CD para modelos ML sobre SageMaker difícil de validar sin cuenta AWS real; builds repetitivos de imágenes por modelo."
solution: "GitHub Actions: lint (ruff), build de 3 imágenes Docker (preprocessing, training, validation), upload de artefactos a S3 (Ministack local) y registro del pipeline vía pipeline.upsert(). Diseño objetivo para producción documentado (AWS real, OIDC, ECR)."
tags: ['aws', 'sagemaker', 'github-actions', 'docker', 'python']
order: 0
status: archived
year: 2024
role: ml ops engineer
type: client
stack: [aws, sagemaker, github-actions, docker, python]
codeSnippet:
  lang: yaml
  code: |
    name: sagemaker-cicd
    on:
      push:
        branches: [model/*]
    jobs:
      build:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - run: docker build -t $IMAGE .
links:
  docs: https://github.com/Luisarg03/sagemaker-cicd-poc/blob/main/docs/design.md
---